[build]
  base = "memory-analysis-dashboard"   # Set the base directory here
  command = "npm run build"            # Build command remains the same
  publish = ".next"                    # Publish directory inside the base

[[plugins]]
  package = "@netlify/plugin-nextjs"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
